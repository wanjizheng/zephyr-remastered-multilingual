# 验证记录

## 0.4.0：三语言正式版

- 本版沿用已获玩家确认的 `0.4.0-local.multilingual.11` 三语言资源操作与字形数据；正式化时将程序及语言包版本标识改为 `0.4.0`，把界面“本地测试版”文案改为正式版，并重新生成固定 SHA-256 与发行包。安装逻辑与资源操作不变。
- 简体、繁体、英文切换及 Steam 恢复状态识别已在隔离游戏副本中通过；英文基础攻击名称修正后的资源完成重新构建和原生 CRC 校验。简体与繁体资源内容保持本地验收包的版本。
- 对安装失败注入异常，验证事务自动回滚；Windows 默认路径长度预算从旧目录的 267 字符缩短到代表性最长相关路径 233 字符。真实 Windows 10 环境和所有玩家安装路径仍需玩家反馈。
- 工具仅在本地修改玩家自己的 Steam 游戏文件。没有把原版游戏资源、存档或测试副本放进发行包。英文是基于简体译文的 GPT 直接翻译，繁体仅转换字形；这些验证不等于完成英文全面排版优化或逐句人工验收。

## 0.2.0：正式版

- 用户已确认 local.2 的本地功能测试通过，授权正式发布。
- 正式版沿用已测试逻辑，工具与数据标识统一为 0.2.0；重建配方、字形数据和目标哈希与验收包一致。
- 用户功能验收不等于所有剧情分支已经逐句人工复核。具体自动检查及历史验证见下方。

## 0.2.0-local.2：Steam 验证自动检测

- 自动定时检测与窗口激活检测接入现有只读资源校验；避免重入，切换目录或关闭窗口停止监测。
- 覆盖等待→原版、Steam 忙时保持等待、Steam 更新后仍不兼容、超时、清单缺失／错误目录，以及清单状态变化测试。
- 已构建自包含单文件程序，等待和原版界面渲染通过。引擎和两个 payload 文件与 local.1 哈希相同。
- 未自动操作玩家 Steam 验证或修改游戏文件；真实 Steam 恢复流程仍由用户验收。

## 0.2.0-local.1：本地验收版，尚未发布

- 19 个声明式输出与已接受累计汉化候选逐字节一致，新增原版模式字体／弹窗与重制版人物名样式。
- 集成测试覆盖正常安装、更新、精确恢复、中断回滚、进程重建恢复、篡改／路径保护、显式强制恢复确认、EXE 更新后恢复、缺失文件恢复，以及 Steam Build 识别。
- 强制恢复不覆盖 EXE，也不把“旧资源恢复成功”误报为“与新版 Steam 游戏兼容”；仍建议验证完整性。
- Material Design 主窗口与嵌入式恢复对话框已离线渲染检查，包括最小窗口尺寸。
- 原版玩家目录已恢复并核验，本轮不自动启动游戏，不上传 GitHub。
- Steam 验证入口仅提交给 Steam，不宣称已替玩家完成下载／验证；本地玩家最终交互验收仍待用户执行。

## 0.1.0-beta.2：说明与语言提示修订

- README 重排，新增 32 项韩文／旧中文／本项目译名对照；新版姓名逐项匹配已生效术语决定，11 项旧名缺少充分证据，保留待核。
- 按 Steam 官方“仅支持韩语”说明修正使用方法及两处程序提示。
- WPF 重新构建，无警告、无错误；版本排序与更新签名检查通过，程序界面离线渲染检查通过。
- 工具版本为 beta.2，汉化数据和资源处理引擎仍为 beta.1；两份 payload 文件及引擎文件保持原 SHA，不需要重新安装相同汉化资源。
- 这次没有修改玩家游戏文件，也没有重新执行或冒充完成一轮全游戏实机验收。资源安装与恢复验证沿用下面的 beta.1 记录。

## 0.1.0-beta.1：资源重建与安装恢复

验证日期：2026-09-20。Windows 11 x64，Python 3.14.7，.NET SDK 10.0.401，随包 .NET Runtime 10.0.12。

通过的检查：

- 原版资源副本可正确识别；声明式重建的 18 个输出文件与累计汉化候选逐字节一致。
- 从原版副本安装后恢复，18 个文件全部回到原始 SHA-256。
- 模拟受管理的旧版安装状态后更新，原版备份不变，输出仍匹配当前候选。该测试覆盖更新路径，不等于另一款尚未存在的旧发行版已经实测。
- 第三个文件写入后注入异常，自动恢复全部原始文件。
- 模拟突然终止留下部分写入及事务记录，重新创建处理器后恢复全部原始文件。
- 游戏文件被其他工具改动时拒绝覆盖；路径越界与发行数据被篡改时拒绝继续。
- 存档哨兵文件未改变；测试副本之外的玩家游戏资源未写入。
- 游戏运行时，安装操作被拒绝。副本集成测试只在自身测试对象上隔离该检测，不向正式程序加入绕过选项。
- PyInstaller 独立引擎完成全量离线重建，不依赖外部 Python 或 Unity 安装。
- 更新版本排序、正确签名、错误 tag、被篡改元数据的检查通过。
- WPF 编译通过，离线界面图已目视检查。

未宣称通过：所有故事分支逐句人工验收、另一台干净 Windows 电脑的交互式安装、所有 DPI／分辨率组合、Windows Authenticode 签名。

当前为公开测试版。玩家实机反馈将用于后续修订。
