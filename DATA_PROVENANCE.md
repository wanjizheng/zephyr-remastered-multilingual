# 发行数据的来源与边界

`payload/zh-Hans/`、`payload/zh-Hant/` 和 `payload/en/` 分别包含三种语言的声明式改动。各目录的 `patch.json.gz` 记录对象身份、原始内容哈希、字段路径、翻译值、样式数值、新字形指标以及本地图集复制坐标。它不是完整资源文件，也不是对压缩游戏资源直接制作的通用二进制差分。v0.2.0 的旧版数据仍保存在历史 tag 中；v0.4.0 不再包含根目录下的旧版数据。

各语言目录的 `glyphs.zip` 仅保存从独立生成的许可字体素材中逐字形取出的压缩像素块。中文使用朱雀仿宋、Noto 系列；英文另使用 Source Sans 3 / Source Serif 4。每块按 SHA-256 命名。导出时要求像素与许可字体来源一致；原游戏字体像素只记录本地复制位置，不放入发行数据。

历史 0.2.0 版覆盖 19 个安装文件中的 2,903 个改变对象；图集重建使用 19,112 次原版字形区域本地复制、81,373 次许可字形块引用，以及原版模式两张 RGBA 图集上的 4,676 次许可 alpha 字形引用。RGBA 原图从玩家原版文件按二维坐标复制，新增白色字形仅从许可字体生成的 alpha 数据构建。引用次数不是去重字符数。保留未修改对象与资源身份。

在受支持原版副本上，完整重建的 19 个输出文件已逐一与当前累计汉化候选进行 SHA-256 比较，全部一致。Bundle 的 Unity 原生 CRC 绑定该最终 SHA；程序只接受这些精确输出，再更新本地 catalog 的 CRC、大小和 hash。没有用安装实测来猜 CRC。

v0.4.0 的简体、繁体和英文数据分别在隔离原版副本上完成资源重建校验；英文基础攻击名称修正后的 Bundle 重新计算了原生 CRC。v0.4.1 进一步将英文探索界面 F9 快捷键标签缩短为 `Auto`，并重新计算受影响 Bundle 的原生 CRC 与 catalog。三个语言包的最终重建哈希由各自的声明式配方固定，安装器会先核对每个语言包的 SHA-256。

v0.4.2-preview.2 在简体与繁体对话字体中补齐“吾”“渝”两字。字形像素由许可的朱雀仿宋分别按两种语言原有的 SDF 设置烘焙；以既有“君”字核对像素与指标。各语言只新增两个许可字形块，保留所有既有字形块及非目标资源；重新核算受影响 Bundle 的 Unity 原生 CRC 与 catalog，并逐一核对 19 个重建输出。

完整的原文库、原始资源、原版备份、生成用 Unity donor bundle、内部游戏测试副本不在公共仓库内。程序没有修改存档的路径，也不包含游戏运行时注入 DLL。

这些技术措施减少官方资源再分发，不代表对游戏译文或原作权利的法律授权。主程序 MIT 与字体 OFL 各有适用范围。


## v0.5.3 视频与二周目提示

当前公开发行包含 060／083／109 的简体和繁体视频，以及 083／109 的英文视频；060 英文原片已是英文，因此沿用原片。视频资源以 `.zvenc` 加密封装，安装时在本机还原；不能把当前发行描述为“不携带任何游戏视频资源”。详见 [视频语言包说明](docs/video-localization.md)。

通关后新游戏窗口使用内嵌字符串；三语配方新增七个字符串的替换操作。安装器按索引和原文哈希核对玩家的原版元数据后重建，不分发完整元数据文件。新旧字符串逐项回读和输出哈希通过，游戏内窗口仍待部署后确认。详见 [修复说明](docs/new-game-after-clear-20260925.md)。
