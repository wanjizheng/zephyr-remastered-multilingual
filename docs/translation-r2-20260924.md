# 2026-09-24 三语审阅 r2 实施

本次只更新补丁源，没有写入游戏、发布安装器或推送远端。

## 已实施

- 合入通过静态验收的 698 个字段：英文 630、简体 34、繁体 34。
- 修复身份揭晓后仍使用早期人物资源的名字栏：12 个场景对象、8 个逻辑场景，三语 36 项。
- 原始 `00chri1f` 继续显示克里斯／Chris；指定后期场景改用 `00chri9f`，名字为克里斯蒂娜／Christina。没有把所有克里斯改名，也没有替换成 09chri2。
- 新名字通过新增人物别名、两套 UnitTexts 名字表与 Addressables 地址接通。角色 ID、动作、精灵信息等非名字字段保持相同。
- 游戏图像加载按场景人物 ID 查找，故同时新增普通／高清精灵别名，复用原纹理、矩形、网格等视觉数据。Unity 原生加载已验证新旧两套图像一致；不只依赖离线 JSON 比较。

## 验证与边界

文本旧值校验、三语资源重建、精确哈希回读、Unity 原生 CRC 与图像加载通过；最终简体完整集成重建通过。原始源表、已验收 r2 ZIP 均保留不改。

698 条文本仍有 12 条相对旧英文的换行差异（5 条物品、7 条旧英文审阅）；数字停顿控制符顺序不变。名字栏修复不靠修改对白，未再增加正文换行。

尚未实际运行游戏验收剧情、存档进入场景、名字显示和事件调度。上述通过不等于游戏实机通过。

## 工作台构建

源码目录仍选择本仓库。本次多了原来未修改的 units 图像资源；已在 `private/review-r2-original` 准备完整、逐文件校验的原版构建输入。工作台的“原版备份目录”选择这个目录即可。它是本地原始输入，不提交 Git、不作为补丁文件公开分发。安装器本身会按原有流程备份新增目标文件。

使用本次源码重新生成引擎与语言包；不要将新语言包交给旧版已编译引擎，因为旧引擎不认识人物及精灵别名操作。

详情见 `translation-r2-20260924-verification.json`；698 条批准文本见 `translation-r2-20260924-fields.jsonl`。源更新前的语言包、校对目录和校验文件保存在本地综合审阅目录下的 `source_update_r2/before`。
